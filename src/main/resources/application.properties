spring.application.name=Backend

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/cinemadb
spring.datasource.username=root
spring.datasource.password=root

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Properties
jwt.secret=tu_clave_secreta_muy_larga_y_segura_para_firmar_los_tokens_jwt_al_menos_256_bits
jwt.expiration=86400000

# Other Configuration
tmdb.api.key=${TMDB_API_KEY}
server.port=8080

# Log SQL queries
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true

# Enable statistics
spring.jpa.properties.hibernate.generate_statistics=true

# Configure batching for improved performance
spring.jpa.properties.hibernate.jdbc.batch_size=30
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Use the default batch fetch size to address N+1 problems
spring.jpa.properties.hibernate.default_batch_fetch_size=25

# Google OAuth2 Configuration
google.client.id=492097747607-1l3j2unsnkp8eq6ugib1bda9truoan1g.apps.googleusercontent.com

# Stripe Configuration
stripe.api.key=sk_test_...
stripe.webhook.secret=whsec_...
stripe.product.id=prod_...
stripe.price.id=price_..
stripe.seat.price=10.00
